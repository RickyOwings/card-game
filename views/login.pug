extends layout.pug

block content
    div.exact-middle
        div.login
            form(action="/login", method="POST", autocomplete="off")
                input(type="text" name="username" placeholder="Insert Username Here" value='' required)
                input(class="interact" type="submit" value="Submit")
            br
    p.t-center Make sure you don't choose someone else's username or pick the same username →
